##################################################################################################
# Productpage services
##################################################################################################
apiVersion: v1
kind: Service
metadata:
  name: productpage-v1
  labels:
    app: productpage-v1
    service: productpage-v1
spec:
  type: NodePort
  ports:
  - port: 3000
    name: http
  selector:
    app: productpage-v1
---
